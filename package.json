{
  "name": "static-site",
  "version": "1.0.0",
  "description": "Simple static site with Markdown support",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "dev": "nodemon build.js & http-server ./dist -c-1"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "gray-matter": "^4.0.3",
    "marked": "^11.1.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "nodemon": "^3.1.10"
  }
}
